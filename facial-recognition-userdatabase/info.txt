.client
 "npm create vite@latest"
  > react
  > javascript
 "npm install"
 "npm run dev"

 "npm install react-router-dom"
 "npm install @mui/material @emotion/react @emotion/styled"

 "npm install yup"
 "npm install formik"

 "npm install axios"

 "npm install react-webcam"


.backend
 >>> Set-ExecutionPolicy Unrestricted -Scope Process
 >>> python -m venv venv
 >>> .\venv\Scripts\activate
 >>> pip install flask
 >>> pip install flask_cors
 >>> pip install flask_bcrypt
 >>> pip install flask_sqlalchemy
 >>> pip install uuid
 >>> pip install pillow # Python Imaging Library
 >>> pip install cmake # dlib was developed in C based programming language, so it needs this to work (also Visual Studio build tools > c++ cmake tools)
 >>> pip install dlib==19.24.2 # dlib needs to be installed to use face_recognition
 >>> pip install face_recognition # Face Recognition Library
 >>> pip install numpy==1.26.4 # Fundamental package for scientific computing in Python
 >>> python app.py